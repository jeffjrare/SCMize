.subnav
  %ul.nav.nav-pills
    %li.active
      %a{:href => "/machines"} Overview
    %li
      %a{:href => "/machines/new"} Create a new machine

%ul.items
  - machines.each do |machine|
    = haml(:'machines/show', :layout => false, :locals => {:machine => machine} )