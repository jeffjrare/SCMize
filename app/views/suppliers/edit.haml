.content
  .page-header
    %h1
      Modifier un fournisseur
      %small <strong>#{current_account.name}</strong> pour <strong>#{current_user.mail}</strong>
  .row
    .span11
      %h2= supplier.name

    .span11
      %ul.breadcrumb
        %li
          %a{:href => "/suppliers"} Fournisseurs
          %span.divider /
        %li.active Modifier un fournisseur
        
      =haml(:'suppliers/_form', :layout => false, :locals => {:supplier => supplier} )
